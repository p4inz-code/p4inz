# P4inz — Gemini Instructions

Follow `AGENTS.md` as the primary project-wide AI-agent policy.

Before implementation:
1. Read `AGENTS.md`.
2. Read `ROADMAP.md`.
3. Read relevant files under `docs/architecture/`.
4. Check existing implementation before creating new code.

Do not redesign locked architecture without an explicit architectural decision.

Security, correctness, privacy, reliability, and maintainability have priority over speed.

Never commit secrets, use destructive commands without authorization, or push changes automatically.
